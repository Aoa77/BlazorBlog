@*
    ArticlePage.razor
*@
@page "/blog/{Key}"

 <DynamicComponent Type="@_data.GetType()" />

@code
{
    private BlogArticle _data => BlogManager.Articles[Key];

    [Parameter]
    public required string Key { get; set; }
}